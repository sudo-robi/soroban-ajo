/**
 * Helper hook to access the theme context.
 * Issue #321: Polished dark mode with smooth transitions
 */
export { useTheme } from '@/context/ThemeContext'
export type { Theme } from '@/context/ThemeContext'
